# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ShowMessage' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ShowMessage
  pod 'Toaster'
  pod 'SnapKit', '~> 4.0.0'
  pod "TTGSnackbar"

  target 'ShowMessageTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShowMessageUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
